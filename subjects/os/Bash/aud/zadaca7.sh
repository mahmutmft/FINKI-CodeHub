# Да се напише командна процедура што ќе ја прикаже содржината на повеќе текстуални датотеки.

files="../aud3/*"

for file in $files
do
    echo "$file"
    cat -n "$file"
    echo "========================="
done